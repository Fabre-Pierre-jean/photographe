{% extends 'base.html.twig' %}

{% block title %}Aboutme{% endblock %}

{% block body %}
    <h1>Aboutme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ aboutme.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ aboutme.title }}</td>
            </tr>
            <tr>
                <th>Desciption</th>
                <td>{{ aboutme.desciption }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('aboutme_index') }}">back to list</a>

    <a href="{{ path('aboutme_edit', {'id': aboutme.id}) }}">edit</a>

    {{ include('aboutme/_delete_form.html.twig') }}
{% endblock %}
